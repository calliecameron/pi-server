Use git-backup-configuration.txt to configure automatic backups of git repositories. Each line in the file should be the URL of a git repo (preferably SSH). The repos must be accessible using this server's deployment key.
