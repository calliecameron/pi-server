- name: Check reachable
  command: ping -c 1 -w 1 -q "{{ item }}"
  changed_when: false
  loop: "{{ reachable }}"

- name: Check unreachable
  command: ping -c 1 -w 1 -q "{{ item }}"
  changed_when: false
  loop: "{{ unreachable }}"
  failed_when: result.rc != 1
  register: result
