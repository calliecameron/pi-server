---
- name: 02-pi-core - 01-vars
  command:
    cmd: "{{ pi_core }}/01-vars"
    stdin: |
      {{ pi_lan_mask }}
      {{ pi_vpn_mask }}

- name: 02-pi-core - 08-openvpn-server
  command:
    cmd: "{{ pi_core }}/08-openvpn-server"
    stdin: |
      y
      y
