- name: define role
  include_tasks: "{{ define_role }}"
  vars:
    _private: false
    _run_once: true
    _args: []
    _host_vars:
      - pi_server_testbed_openvpn_target_name
      - pi_server_testbed_openvpn_target_ip
    _export_vars: []
