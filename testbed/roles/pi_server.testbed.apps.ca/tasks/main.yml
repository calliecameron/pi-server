- name: define role
  include_tasks: "{{ define_role }}"
  vars:
    _private: false
    _run_once: true
    _args: []
    _host_vars:
      - pi_server_testbed_pi1_ip
      - pi_server_testbed_pi2_ip
      - pi_server_testbed_ubuntu_ip
    _export_vars: []
