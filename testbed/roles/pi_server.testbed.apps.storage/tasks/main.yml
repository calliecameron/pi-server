- name: define role
  include_tasks: "{{ define_role }}"
  vars:
    _private: false
    _run_once: true
    _args: []
    _host_vars:
      - pi_server_testbed_storage_drive
      - pi_server_storage_zpool
    _export_vars: []
