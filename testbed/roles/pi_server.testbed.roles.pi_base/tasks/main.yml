- name: define role
  include_tasks: "{{ define_role }}"
  vars:
    _private: False
    _run_once: True
    _args: []
    _host_vars:
      - pi_server_lan_iface
      - pi_server_testbed_router_ip
    _export_vars: []
