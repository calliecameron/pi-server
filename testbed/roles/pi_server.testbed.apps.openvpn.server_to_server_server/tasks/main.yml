- name: Define role
  ansible.builtin.include_tasks: "{{ define_role }}"
  vars:
    _private: false
    _run_once: true
    _args: []
    _host_vars:
      - pi_server_testbed_openvpn_client_name
      - pi_server_testbed_openvpn_client_lan_network
      - pi_server_testbed_openvpn_client_vpn_network
    _export_vars: []
