- name: define role
  include_tasks: "{{ define_role }}"
  vars:
    run_once: False
    args:
      - dest_dir
      - head_title
      - body_title
      - fragments_notify
    export_vars:
      - pi_server_utils_page_templates_bootstrap_version
