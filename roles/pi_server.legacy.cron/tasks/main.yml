- name: define role
  include_tasks: "{{ define_role }}"
  vars:
    run_once: True
    export_vars:
      - pi_server_legacy_cron_etc
      - pi_server_legacy_cron_safe_dir
      - pi_server_legacy_cron_normal_dir
      - pi_server_legacy_cron_pause_dir
