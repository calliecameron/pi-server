- ansible.builtin.include_role:
    name: pi_server.base.hostname

- ansible.builtin.include_role:
    name: pi_server.base.localisation

- ansible.builtin.include_role:
    name: pi_server.base.packages

- ansible.builtin.include_role:
    name: pi_server.base.cleanup_users

- ansible.builtin.include_role:
    name: pi_server.base.os_hardening

- ansible.builtin.include_role:
    name: pi_server.apps.email

- ansible.builtin.include_role:
    name: pi_server.apps.ssh

- ansible.builtin.include_role:
    name: pi_server.apps.firewall

- ansible.builtin.include_role:
    name: pi_server.apps.docker

- ansible.builtin.include_role:
    name: pi_server.apps.traefik

- ansible.builtin.include_role:
    name: pi_server.apps.monitoring

- ansible.builtin.include_role:
    name: pi_server.apps.cron

- ansible.builtin.include_role:
    name: pi_server.apps.updates
