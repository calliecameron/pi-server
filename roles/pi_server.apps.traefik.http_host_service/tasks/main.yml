- name: Define role
  ansible.builtin.include_tasks: "{{ define_role }}"
  vars:
    _private: false
    _run_once: false
    _args:
      - name
      - entrypoint
      - path_prefix
      - strip_prefix
      - port
      - https
      - https_entrypoint
    _host_vars: []
    _export_vars: []
