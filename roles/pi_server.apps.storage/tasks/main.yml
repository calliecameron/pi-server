- name: define role
  include_tasks: "{{ define_role }}"
  vars:
    run_once: True
    host_vars:
      - pi_server_storage_drive
      - pi_server_storage_data_partition
      - pi_server_storage_backup_partition
      - pi_server_storage_spinning_drive
    export_vars:
      - pi_server_apps_storage_data_mount_dir
      - pi_server_apps_storage_backup_mount_dir
