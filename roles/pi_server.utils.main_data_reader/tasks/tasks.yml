- include_role:
    name: pi_server.apps.main_data

- name: "add {{ user }} to main data readers"
  become: yes
  ansible.builtin.user:
    name: "{{ user }}"
    groups:
      - "{{ pi_server_apps_main_data_reader_group }}"
    append: yes
