- name: define role
  include_tasks: "{{ define_role }}"
  vars:
    _private: False
    _run_once: False
    _args:
      - fragments_dir
      - web_root
      - temp_dir
      - head_title
      - body_title
      - favicon_basename
      - fragments_notify
    _host_vars: []
    _export_vars: []
