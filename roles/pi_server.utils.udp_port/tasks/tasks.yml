- include_role:
    name: pi_server.apps.firewall

- name: "open udp {{ port }} at boot"
  become: yes
  community.general.ufw:
    port: "{{ port }}"
    proto: udp
    rule: allow
  notify: reload firewall
