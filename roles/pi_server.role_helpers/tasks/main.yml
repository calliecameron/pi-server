- name: define role
  include_tasks: "{{ define_role }}"
  vars:
    _private: False
    _run_once: True
    _args: []
    _host_vars: []
    _export_vars:
      - define_role
      - _pi_server_role_helpers_define_role_internal
