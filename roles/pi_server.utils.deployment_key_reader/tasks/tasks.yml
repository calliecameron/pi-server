- include_role:
    name: pi_server.apps.certs

- name: "add {{ user }} to deployment key readers"
  become: yes
  ansible.builtin.user:
    name: "{{ user }}"
    groups:
      - "{{ pi_server_apps_certs_deployment_key_readers_group }}"
    append: yes
