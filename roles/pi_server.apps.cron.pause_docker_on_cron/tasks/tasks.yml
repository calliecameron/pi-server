- include_role:
    name: pi_server.apps.cron.base

- name: "pause docker '{{ service }}' on cron"
  include_role:
    name: pi_server.utils.create_file
  vars:
    path: "{{ pi_server_apps_cron_pause_dir }}/{{ service }}.docker"
    owner: root
    group: root
    mode: a=r
