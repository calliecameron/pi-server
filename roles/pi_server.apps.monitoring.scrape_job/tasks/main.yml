- name: Define role
  ansible.builtin.include_tasks: "{{ define_role }}"
  vars:
    _private: false
    _run_once: false
    _args:
      - job
      - host
      - port
      - metrics_path
    _host_vars: []
    _export_vars: []
