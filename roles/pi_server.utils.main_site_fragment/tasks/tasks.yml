- include_role:
    name: pi_server.apps.main_site_base

- name: fragment
  become: yes
  ansible.builtin.template:
    src: "{{ src }}"
    dest: "{{ pi_server_apps_main_site_fragments_dir }}/01-{{ dest }}"
    owner: root
    group: root
    mode: a=r
  notify: "assemble main site"
