- name: "assemble main site"
  become: yes
  ansible.builtin.assemble:
    src: "{{ pi_server_apps_main_site_fragments_dir }}"
    dest: "{{ pi_server_apps_main_site_web_root }}/index.html"
    owner: root
    group: root
    mode: u=rw,go=r
  listen: "assemble main site"
