- include_role:
    name: pi_server.apps.firewall

- name: enable forwarding in the firewall
  become: yes
  ansible.builtin.copy:
    content: ""
    dest: "{{ pi_server_apps_firewall_allow_forwarding }}"
    owner: root
    group: root
    mode: u=rw,go=r
  notify: reload firewall
