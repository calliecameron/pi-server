- name: define role
  include_tasks: "{{ define_role }}"
  vars:
    run_once: False
    args:
      - script_job
      - script_src
      - script_dest_dir
      - script_user
      # may optionally set 'script_conflicts'
    host_vars: []
    export_vars: []
