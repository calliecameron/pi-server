- name: define role
  include_tasks: "{{ define_role }}"
  vars:
    _private: false
    _run_once: false
    _args:
      - job
      - src
      - dest_dir
      - user
      - systemd_conflicts
      - docker_conflicts
    _host_vars:
      - pi_server_hostname
    _export_vars: []
