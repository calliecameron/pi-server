- include_role:
    name: pi_server.apps.monitoring

- name: custom dashboard
  become: yes
  ansible.builtin.copy:
    src: "{{ src }}"
    dest: "{{ pi_server_apps_monitoring_grafana_dashboards }}/{{ src }}"
    owner: root
    group: root
    mode: a=r
