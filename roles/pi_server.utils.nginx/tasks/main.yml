- name: define role
  include_tasks: "{{ define_role }}"
  vars:
    _private: False
    _run_once: False
    _args:
      - name
      - entrypoint
      - compose_dir
      - uid
      - gid
      - web_root
    _host_vars: []
    _export_vars: []
