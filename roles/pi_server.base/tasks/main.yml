- name: define role
  include_tasks: "{{ define_role }}"
  vars:
    run_once: True
    args: []
    host_vars: []
    export_vars:
      - pi_server_etc
      - pi_server_opt
      - pi_server_var
      - pi_server_net_pre_up
      - pi_server_net_up
