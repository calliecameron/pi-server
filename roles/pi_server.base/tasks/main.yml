- name: define role
  include_tasks: "{{ define_role }}"
  vars:
    _private: False
    _run_once: True
    _args: []
    _host_vars: []
    _export_vars:
      - pi_server_etc
      - pi_server_opt
      - pi_server_var
      - pi_server_net_pre_up
      - pi_server_net_up
