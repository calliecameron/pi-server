- include_role:
    name: pi_server.base

- name: "user '{{ args.user }}'"
  become: yes
  ansible.builtin.user:
    name: "{{ args.user }}"
    password: !
    password_lock: yes
    shell: /usr/sbin/nologin
    create_home: no
