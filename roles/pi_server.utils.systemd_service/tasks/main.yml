- name: define role
  include_tasks: "{{ define_role }}"
  vars:
    _private: False
    _run_once: False
    _args:
      - src
      - service
      - service_notify
    _host_vars: []
    _export_vars: []
