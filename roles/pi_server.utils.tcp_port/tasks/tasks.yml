- include_role:
    name: pi_server.apps.firewall

- name: "open tcp {{ port }} at boot"
  become: yes
  community.general.ufw:
    port: "{{ port }}"
    proto: tcp
    rule: allow
  notify: reload firewall
