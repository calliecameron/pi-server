- name: reload shutdownd
  become: yes
  ansible.builtin.systemd:
    name: "{{ pi_server_apps_shutdownd_service }}"
    daemon_reload: yes
    enabled: yes
    state: restarted
  listen: "reload shutdownd"

- name: shutdownd noop
  ansible.builtin.meta: flush_handlers
  listen: "shutdownd noop"
